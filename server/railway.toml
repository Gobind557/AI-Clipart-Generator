# Use this service as a **Node API**, not Expo. In Railway: set Root Directory to `server`
# (repo folder containing this file), then redeploy.

[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/v1/health"
healthcheckTimeout = 300
